import BaseButton from "../common/components/button.component.js";

class ErrorButton extends BaseButton {
    constructor(rootSelector){
        super(rootSelector);
    }
}

export default ErrorButton;